#!/usr/bin/env bash
#MISE description="Print the current project version"

set -euo pipefail

repo_root=$(git rev-parse --show-toplevel 2>/dev/null) || {
  printf 'error: run from a Git worktree\n' >&2
  exit 1
}
cd "$repo_root"

uv version --short
